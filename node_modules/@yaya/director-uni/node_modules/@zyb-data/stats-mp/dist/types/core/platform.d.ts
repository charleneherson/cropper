export interface StatsPlatformRequestOption {
    url: string;
    data?: string;
    query?: string;
    success?: (res: any) => any;
    fail?: (res: any) => any;
    complete?: (res: any) => any;
    headers?: object;
    ext?: object;
}
/**
 * 平台抽象类
 * 抽象平台的基本能力，不同平台具体实现
 * sdk core使用统一的平台接口
 */
export declare abstract class StatsPlatform {
    abstract parseURL(url: string): any;
    abstract getCUID(): Promise<string>;
    abstract getNetworkInfo(): Promise<any>;
    abstract getSystemInfo(): Promise<any>;
    abstract request(option: any): Promise<any>;
    abstract setStore(key: string, value: any): any;
    abstract getStore(key: string): any;
    abstract getHitTargets(x: number, y: number, elements: string[], ctx?: any, target?: any): Promise<any[]>;
    abstract getVisibleTargets(elements: string[], ctx?: any): Promise<{
        hit: any[];
        unhit: any[];
        list: any[];
    }>;
    abstract sendLog(option: any): Promise<any>;
}
