import { DataPoolManager } from './';
import StatsMain from '../core';
export declare class StatePageHybrid {
    private $dm;
    private $sm;
    private route;
    private query;
    private visionCheckTimerID;
    private visionCheckTimeout;
    private visionCache;
    private _visionCheck?;
    private events;
    constructor($dm: DataPoolManager, $sm: StatsMain);
    init(): void;
    /**
     * 视图变化
     */
    visionCheck(): void;
    clearVisionCheckTimer(): void;
    eventHandler(): (this: any, event: any) => void;
    private parseUrl;
    private setContext;
}
