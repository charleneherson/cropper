import { StatsPlatform } from '../core/platform';
export declare class StatsPlatformMp implements StatsPlatform {
    private systemInfo;
    private networkInfo;
    private storePrefix;
    private cuidStoreKey;
    parseURL(url: string): any;
    getCUID(): Promise<string>;
    private cuid;
    getNetworkInfo(): Promise<any>;
    getSystemInfo(): Promise<any>;
    request(option: any): Promise<any>;
    sendLog(option: any): Promise<any>;
    setStore(key: string, value: any): any;
    getStore(key: string): any;
    getHitTargets(x: number, y: number, elements: string[], ctx: any, target: any): Promise<any[]>;
    getVisibleTargets(elements: string[]): Promise<{
        hit: any[];
        unhit: any[];
        list: any[];
    }>;
    private getBoundingClientRect;
    private getViewportInfo;
    private isPointInTarget;
    private isTargetInViewport;
}
