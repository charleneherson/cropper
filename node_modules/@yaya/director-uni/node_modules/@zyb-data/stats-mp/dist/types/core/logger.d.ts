import { StatsPlatform } from './platform';
import { StatsConfigService } from './config';
export declare class StatsLogger {
    private $platform;
    private $config;
    constructor($platform: StatsPlatform, $config: StatsConfigService);
    send(data: any): any;
}
