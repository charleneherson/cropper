import { StatsConfig } from '../core/config';
import { DataType } from '@zyb-data/stats-core';
/**
 * hybrid 的 dataPool 与其它的不同，
 * 是由 native 管理的，所以需要单独实现
 */
export declare class DataPoolManager {
    /**
     * 向native的数据池中设置数据
     */
    setDataPool(data: {
        type: DataType;
        value: {
            [key: string]: any;
        };
    }): Promise<void>;
}
declare class ZybStats {
    private cfg;
    private $dm;
    private $page;
    private sm;
    constructor();
    /**
     * 更新配置
     * @param config
     */
    config(config: StatsConfig): void;
    /**
     * sdk 初始化
     */
    init(): void;
    /**
     * 更新全局参数
     * @param {String} params 全局参数（增量覆盖）
     */
    global(params: {
        [key: string]: any;
    }): Promise<void>;
    /**
     * 手动打点方法
     * @param {String} params 打点参数
     */
    track(name: string, params?: {
        [key: string]: any;
    }): Promise<unknown>;
}
export declare const zs: ZybStats;
export default zs;
