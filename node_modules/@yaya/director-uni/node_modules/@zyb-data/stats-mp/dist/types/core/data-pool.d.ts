import { DataPool, GlobalData, DataDescriber, DataType, EventData, NetworkData, DataPage } from '@zyb-data/stats-core';
import { StatsConfigService } from './config';
/**
 * 数据值的合并类型
 */
declare type DataValue = EventData | NetworkData | {
    [paramsName: string]: any;
} | any;
/**
 * 数据池管理器
 */
export declare class StatsDataPoolManager {
    private $config;
    /**
     * 数据池
     */
    private dataPool;
    /**
     *
     * @param $config 配置服务
     */
    constructor($config: StatsConfigService);
    /**
     * 插入新页面数据
     * @param pid 页面id
     */
    appendDataPage(pid?: string): DataPage;
    /**
     * 创建页面数据
     * @param pid 页面id
     */
    createDataPage(pid?: string): DataPage;
    /**
     * 获取页面数据
     * @param lb lookback 回溯层级 @default 0
     */
    getDataPage(lb?: number): DataPage;
    /**
     * 获取当前页面数据
     */
    getCurrentDataPage(): DataPage;
    /**
     * 设置数据
     * @param dataType 数据类型
     * @param value 数据值
     * @param cp 当前页面上下文数据
     */
    setData(dataType: DataType, value: DataValue, cp: DataPage): DataValue;
    /**
     * 获取数据
     * @param dd 数据描述
     * @param cp 当前页面上下文数据
     */
    getData(dd: DataDescriber, cp?: DataPage): DataValue;
    getPool(): DataPool;
    get $dp(): Array<DataPage>;
    get $global(): GlobalData;
    set $global(gl: GlobalData);
    /**
     * 设置全局数据
     * @param data kv
     */
    setGlobal(data: any): void;
    get $common(): {};
    get $currentPage(): DataPage;
    /**
     * 对obj进行任意层级的安全取值方法
     *
     * @param obj 目标对象
     * @param ks keys
     */
    private access;
    private searchEvents;
    private searchEvent;
    private searchNetworks;
    private searchNetwork;
}
export {};
