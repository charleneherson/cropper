import { StatsConfig } from '../core/config';
declare class ZybStats {
    private cfg;
    private $page;
    private sm;
    constructor();
    /**
     * 更新配置
     * @param config
     */
    config(config: StatsConfig): void;
    /**
     * 设置、更新全局变量
     * @param params
     */
    global(params: {
        [key: string]: any;
    }): void;
    /**
     * 设置openid=》cuid
     * @param openid
     */
    setCuid(openid: string, updateStore?: boolean): void;
    /**
     * 获取 cuid
     */
    getCuid(): string;
    /**
     * 设置登陆用户id（作业帮用户id，加密）
     * @param uid
     */
    login(uid?: string, updateStore?: boolean): void;
    /**
     * 初始化
     */
    init(): void;
    /**
     * 主动埋点方法
     * @param name 点位名字
     * @param params 点位参数
     */
    track(name: string, params: object): void;
    /**
     * 页面呈现主动埋点
     */
    pageShow(url?: string, rule?: string): void;
    /**
     * 页面隐藏主动埋点
     */
    pageHide(url?: string, rule?: string): void;
}
export declare const zs: ZybStats;
export {};
