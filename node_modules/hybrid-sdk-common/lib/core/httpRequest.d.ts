import { Method } from 'axios';
interface requestParams {
    type: Method;
    url: string;
    headers?: object;
    params?: object;
    [propName: string]: any;
}
interface callback {
    (): any;
}
/**
 * 通过native调用接口
 * @param params
 */
export declare function core_request(params: requestParams): any;
/**
 * 通过native调用接口, 适配了之前的API
 * @param params
 */
export declare function request(params: any, callback?: callback): any;
export {};
