/**
 * 将服务端动态下发的url，根据平台判断是否需要转换为zybhost私有协议
 * 如果一个前端模块需要接入sparta的动态资源预加载，则需要业务方在每次获取到服务端的动态下发url后，经过此函数进行转换
 * @param {sting} url
 */
export declare function transformCacheUrl(url: string): string;
