import { getPromiseFunc } from "../utils/tool";

/**
 * 数据埋点
 * @param params
 */
export function nativeLog(params: any): any {
    let _params = {
        ...params
    }

    return getPromiseFunc("core_nlog", _params);
}