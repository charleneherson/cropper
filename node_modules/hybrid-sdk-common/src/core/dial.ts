import { getPromiseFunc } from "../utils/tool";

interface dialParams {
    phone: String;  // 电话号码
    businessData?: any;
}

/**
 * 吊起手机拨号界面
 * @param params 
 */
export function dial(params: any): any {

    return getPromiseFunc("core_dial", params);
}