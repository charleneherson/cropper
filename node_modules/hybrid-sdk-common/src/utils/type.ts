/**
 * @description 类型检查
 * @param {Object} parameters 参数
 * @example
 * Type.isArray([]);//true
 * Type.isFunction(function(){});//true
 * Type.isString(function(){});//true
 *  */

let Type: any = {}

var typeArray = ['String', 'Number', 'Array', 'Function']

for (let i = 0, len = typeArray.length; i < len; i++) {
  var type = typeArray[i];
  (function (type) {
    Type['is' + type] = function (parameters: any) {
      return Object.prototype.toString.call(parameters) === '[object ' + type + ']'
    }
  })(type)
}

export default Type
