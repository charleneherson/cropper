import {resolveCallbackFactory, rejectCallbackFactory} from "../utils/tool";
import {core_getCommonParameters} from  'hybrid-sdk-common'

export function getCommonParameters(callback?: Function){
    let success= resolveCallbackFactory(callback);
    let fail= rejectCallbackFactory(callback);
    return core_getCommonParameters().then(success, fail); 
}