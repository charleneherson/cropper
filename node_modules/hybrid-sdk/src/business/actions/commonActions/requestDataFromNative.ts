import { hybrid as handleHybrid } from '../../../utils/iframeHybrid'

/**
 * @description 从native获取页面的数据
 */
export function requestDataFromNative (callback: Function) {
  (<HybridFunc>handleHybrid)('requestDataFromNative', callback)
}
